# Results

`metrics/` contains aggregate metrics for the main experiment suites. `tables/` contains the compact CSV and LaTeX tables used to report the paper results.

The public package intentionally excludes raw OULAD files, serialized sequence caches, model checkpoints, and per-learner prediction outputs.

